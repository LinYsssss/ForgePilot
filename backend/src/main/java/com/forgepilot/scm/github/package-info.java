/**
 * GitHub's wire protocol: the REST client, the webhook endpoint and the filter
 * chain that endpoint needs. One of the two provider subpackages ARCHITECTURE.md
 * 1.1 sanctions; everything provider-neutral stays in {@code scm} itself.
 */
package com.forgepilot.scm.github;
