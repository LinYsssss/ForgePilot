# Trivy 抑制清单。写在这里的每一条都必须带到期日(exp:)与理由——到期后自动恢复拦截,
# 逼迫复查,避免抑制变成永久的静默。
#
# 门禁口径见 deploy/scan-images.sh:HIGH/CRITICAL 且「上游已发布修复版本」才拦截。
# 下面各条正好卡在该口径的盲区:Go 侧确实已发布修复(1.26.6),但修复尚未流入任何
# 可拉取的 docker CLI 镜像,于是 Trivy 判定"可修复"而实际上无版本可升——门禁若一直红,
# 结果是所有人学会忽略这个 job,比一条有到期日的显式抑制更危险。
#
# 命中位置:sandbox-runner 镜像内的 /usr/local/bin/docker(gobinary),全部来自该二进制
# 静态链接进去的 Go 标准库,**不是本项目自己的依赖**。该二进制由 docker:*-cli 提供,
# sandbox-runner 通过它驱动 docker create/start/wait(见 DockerCliClient),
# 只与本机 docker.sock(Unix socket)通信:
#   - 不监听端口、不作为服务端处理外部请求;
#   - 不向远端 daemon 发起 TLS 连接(故不解析外来证书链);
#   - 命令行参数全部由本项目确定性代码拼装,不含攻击者可控字符串;
#   - 不渲染 HTML 模板、不解析外部 XML。
# 因此下列漏洞所在代码路径在本项目用法下均不可达或不受攻击者影响,实际风险低。
#
# 现状(2026-08-15 复核):
#   docker/cli 最新 tag 仍是 v29.7.2,ARG GO_VERSION=1.26.5;master 已 bump 到 1.26.6,
#   但尚未有任何已发布 tag(v29.8/v30 均不存在)带上它。
# 解除条件:上游发布带 Go>=1.26.6 的 docker:*-cli 后,升级 sandbox-runner/Dockerfile
#   的 docker-cli 阶段并**整体删除本文件的 CVE 行**——不要只删能对上号的那几条。
#
# 到期日统一为 2026-11-01:让所有抑制在同一天恢复拦截,只需一次复查,
# 避免错开日期导致反复被单条打断。

# ---- 2026-08-14 登记:x/net 相关 ----
# stdlib/x-net idna punycode 提权,修复版 Go 1.26.6 / 1.27.0-rc.3
CVE-2026-39821 exp:2026-11-01

# x/net dnsmessage 解析 DoS,修复版同上
CVE-2026-46600 exp:2026-11-01

# ---- 2026-08-15 登记:Trivy 漏洞库当日新增的 Go stdlib 批次 ----
# 这批 6 条与上面两条同源(同一个 Go 1.26.5 二进制),同随 1.26.6 修复。
# 注意:它们是**漏洞库更新**带出来的,不是本仓库代码变更引入的——
# 2026-08-15 07:34 的 CI 还是绿的,同一镜像未变,12:20 就红了。

# encoding/asn1 解析 DoS。CLI 走 unix socket,不解析外来证书/ASN.1
CVE-2026-33818 exp:2026-11-01

# net/http 明文 HTTP/2(h2c)。CLI 不监听端口、不作服务端
CVE-2026-56853 exp:2026-11-01

# html/template XSS。CLI 只用 text/template 做 --format,不渲染 HTML
CVE-2026-56858 exp:2026-11-01

# encoding/xml 解析 DoS。本链路无外部 XML 输入
CVE-2026-56859 exp:2026-11-01

# net/url 解析 DoS。URL 由本项目代码拼装,非攻击者可控
CVE-2026-56860 exp:2026-11-01

# crypto/tls 握手 DoS。CLI 只连本机 docker.sock,不发起 TLS
CVE-2026-56862 exp:2026-11-01
