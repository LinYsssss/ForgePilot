# Evaluation

这里用于保存 ForgePilot V2 的版本化评测清单、评分器和可重算结果。

论文核心对比：

1. `Diff + LLM`
2. `Diff + Requirement + Acceptance Criteria + LLM`
3. `Diff + Requirement + Acceptance Criteria + Project Knowledge + LLM`

至少报告 Precision、Recall、误报率、漏报率、需求违规召回率、Token 和耗时。旧语料与工具位于 RepoSage，按迁移矩阵选择性引入。
